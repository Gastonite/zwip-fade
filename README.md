# zwip
Yet another js animation engine

## Run the example

```
cd example/simple

npm install && npm run dev

```

or, with yarn:
```
cd example/simple

yarn && yarn dev

```
and [open the example](http://localhost:3000)